services:
  fintracker:
    image: ghcr.io/${GHCR_REPOSITORY:-your-org-or-user}/fintracker:${FT_IMAGE_TAG:-prod}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${FT_PUBLIC_PORT:-8008}:${PORT:-8000}"
    environment:
      PORT: ${PORT:-8000}
